# Saptarshi Nath — Personal Academic Website

A lightweight, dependency-free personal website designed for GitHub Pages.

## Deploy on GitHub Pages

### Option 1: User site (`username.github.io`)

1. Create a GitHub repository named exactly `YOUR_USERNAME.github.io`.
2. Upload the contents of this folder to the repository root.
3. Commit and push to the `main` branch.
4. In GitHub, open **Settings → Pages**.
5. Under **Build and deployment**, choose **Deploy from a branch**, select `main` and `/ (root)`, then save.
6. Your site will be available at `https://YOUR_USERNAME.github.io/`.

### Option 2: Project site

1. Create any public repository, for example `personal-website`.
2. Upload these files to the repository root and push to `main`.
3. In **Settings → Pages**, deploy from the `main` branch and `/ (root)`.
4. The site will appear at `https://YOUR_USERNAME.github.io/personal-website/`.

## Edit the site

- Main content: `index.html`
- Design and responsive layout: `assets/styles.css`
- Tiny footer-year helper: `assets/main.js`

No package manager, framework, or build command is required.

## Optional next steps

- Replace the `SN` monogram card with a portrait you own by adding the image to `assets/`.
- Add a downloadable CV to the repository and link it from the hero.
- Add a custom domain in GitHub Pages settings.
- Keep publication links up to date; Google Scholar is linked for live citation metrics.
